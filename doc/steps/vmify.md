---
description: This Step will Compile your script and run it within a VM.
---

# Vmify

### Settings

- `MaxStatements` (number, default: `1000`): Maximum number of statements allowed in a hardened VM dispatcher block before it is split into linked chunks. Lower values create smaller dispatcher chunks; higher values keep larger blocks together.
